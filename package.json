{
  "name": "azkena",
  "version": "0.1.0",
  "description": "MCP server for Pilotariak — exposes Basque pelota competition data to AI assistants",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "test": "vitest",
    "types": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.14.7",
    "@cloudflare/workers-types": "^4.20250409.0",
    "typescript": "^5.8.3",
    "vitest": "^4.1.0",
    "wrangler": "4.84.1"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "zod": "^3.23.8"
  }
}
